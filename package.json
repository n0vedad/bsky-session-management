{
  "name": "bsky_session_management",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "start": "npm run clean && dist/index.js",
    "dev": "npm run clean && tsc && node dist/index.js",
    "test": "jest"
  },
  "dependencies": {
    "@atproto/api": "^0.12.19",
    "dotenv": "^16.4.5",
    "http": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "jest-mock-extended": "^3.0.7",
    "supertest": "^7.0.0",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
